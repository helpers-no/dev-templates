# template-info.yaml — atlas application catalogue entry
#
# This is the CATALOGUE POINTER, not the install definition. The definition is the
# OCI artifact named in `source:` below, published by atlas beside its own image.
# `params:` and `provides:` deliberately do not appear here — they come from the
# artifact's own template-info.yaml. See ../README.md.

id: atlas
version: "v20260919-6e10058"
name: Atlas Data
description: >-
  Norway's civil-society sector as a REST API — municipality-level indicators of
  humanitarian need, and the NGOs and local chapters that respond to them.
category: APPLICATION
install_type: application
# Authored by atlas (urb-agents #489), not by this repository. Replace only on
# atlas's word.
abstract: |-
  Installs the Atlas data platform as one application: a PostgreSQL database with
  its migrations, a Dagster code location running the ingest and dbt transforms,
  and a PostgREST API over the curated api_v1 views.

  The data is an open semantic layer over Norwegian public sources — SSB
  statistics, FHI public-health and living-conditions indicators, Bufdir child-poverty
  figures and the Brønnøysund business register — joined to NGO supply data, so
  that humanitarian need and the organisations responding to it can be queried
  together at kommune level.

  The API is self-describing: meta_sources, meta_endpoints and meta_dimensions let
  a consumer discover what is available without out-of-band documentation.

  A fresh install serves an empty API until the first pipeline run — the schema and
  grants exist from install, the data arrives on the ingest schedule. Exports
  api-url for other applications to consume.

  Installing does not start anything: the schedules and sensors ship stopped, so
  no data is fetched until an operator turns them on. That is a deliberate go-live
  decision, not a side effect of installing. Nothing is ever backfilled — but the
  timing differs between the two halves and only one of them waits: the schedules
  wait for their next scheduled time, while the automation sensor that drives the
  ~40 ingest sources can begin fetching within about a minute of being enabled.
  Launch the first-data jobs BEFORE enabling automation.
tools: ""
readme: README-atlas.md
tags:
  - atlas
  - ngo
  - data
  - postgresql
  - dagster
  - postgrest
  - api
logo: atlas-logo.svg
maintainers:
  - terchris
links:
  - url: https://github.com/terchris/atlas
    title: Source code
    icon: github
  # Written from ops's production install; its "Things that look like failure and are not"
  # section covers the transform_checks slowness that cost three people a wrong conclusion.
  # Checked by hand (HTTP 200) -- no pipeline stage validates an external link.
  - url: https://github.com/terchris/atlas/blob/main/website/docs/contributors/installing-on-uis.md
    title: Installing on UIS
    icon: github

# Where the install definition actually lives.
#
# The digest is authored and committed, never resolved at build time: a tag is
# mutable at a registry, so a build that re-resolved it would let a re-pointed tag
# be blessed by the next unrelated docs build. Bumping atlas is a reviewed change
# to `digest` here.
#
# Verified before pinning by `scripts/verify-application-pin.sh` (urb-agents #1172),
# PASS on 2026-09-19 for v20260919-6e10058:
#   release asset artifact_digest == GHCR docker-content-digest == --expect-digest
#   image_digest (sha256:98026678…) distinguished and asserted NOT to be this value
#   blob hashes to the manifest; 45082 bytes via the raw-UTF-8 path
#   artifact self-identifies kind=application id=atlas tag=v20260919-6e10058
#   the digest is independently pullable BY DIGEST (HTTP 200), not only by tag
#
# 🟢 Nominated by atlas and validated against the LIVE API by ops-dev (#1261), not
# reported: barn_i_lavinntekt sums 124,669 over 357 kommuner for 2024 — atlas predicted
# 124,670 if correct and ~1,097,107 if the operands were inverted, so the child-poverty
# column this whole day started from is right. dim_kommune published at 1,158 rows (357
# active plus history, as predicted). indicator_summary carries upstream_title, publisher,
# eu_theme and tags. The ANALYZE post-hook is proven to have EXECUTED by re-measuring the
# consumer's own finding: ngo_index estimated 11 / exact 11 and meta_sources 44 / 44, where
# both were wildly out before.
#
# ⚠️ ONE THING NOT CONFIRMED, and it is deliberately named rather than omitted:
# transform_checks reporting the relationships test green is UNCONFIRMED. atlas shipped a
# relationships TEST rather than a foreign-key constraint, on purpose, because an enforced
# FK fails the nightly transform instead of a test. That test being green is a precondition
# for ADDING the constraint later — it is not evidence about whether this build is correct,
# and the embed returning 400 is expected rather than a regression.
#
# ⚠️ "cannot be launched" STILL appears in this artifact and it is NOT the old false
# claim. Checked rather than assumed: it occurs only inside the artifact's own retraction
# of what it said until 2026-09-16. The live text now reads "All 685 can be run on demand"
# with measured launches of 364 s and 885 s to start, and names the real hazard —
# re-launching a run that looks hung produces DUPLICATE runs.
#
# 🔴 EXIT-CODE CONTRACT CHANGE, on the page where a gating caller meets it: a dark
# Automation block returns exit 0 on this build and returned exit 2 on e439668. Deliberate
# — an exit code reporting the tool's own wiring is not a health gate — and the build says
# so when it happens, naming the supported way to tighten it.
#
# ✅ Retired by this pin, exactly as its condition said: the transform_checks note added at
# 18:00 against e439668's false launch text. The condition was "the pin moves off
# e439668", and it fired.
#
# ⚠️ KEPT, deliberately: the LOADED-but-NOT-VALIDATED box. Its condition — the installer
# rendering the version this host installed — IS now satisfied, and the sentence appears in
# the install summary. It stands as corroboration because one operator has seen it on one
# host, and it costs a reader nothing until a second confirms.
#
# 🔴 DAGSTER_DATABASE_URL is now read NOWHERE. Both previously-dark blocks answer through a
# plain `uis template check atlas` with no credential exported. This entry never documented
# that remedy, so nothing here needed unwinding — worth noting because the fix removed the
# requirement rather than documenting it.
#
# ⚠️ NOT verified by imac, volunteered rather than found: #311 half 1 and #309's
# deletion-propagation line both need a Brreg deletion in flight and Brreg published none
# in the window — imac tried to fabricate one, hit a foreign key, and STOPPED rather than
# route around it, because a synthetic row proves the check works on synthetic rows. Also
# unshown: an HTTP 400 rejection path, and multi-location disambiguation on a
# single-location install. #317 was again a threshold-scaled reproduction.
#
# 🔵 Not part of this build's account: a Dagster QueuePool timeout on a large asset-check
# query, and a transform_checks launch measured at 885 s against a 900 s ceiling — fifteen
# seconds of margin. No timeout occurred in imac's pass, but on a busy cluster it would
# present as an image-pull or scheduling failure rather than as a timeout. That is on the
# page.
source:
  artifact: ghcr.io/terchris/atlas-data/uis
  tag: v20260919-6e10058
  digest: "sha256:1875211c2826c931ea12f125719a1dc83f9465933d5716daa413ae1dca76f5d1"

# Top-level, NOT inside source: that is where UIS reads it from.
visibility: public

configure_command: "uis template install atlas"

prerequisites:
  - text: "UIS provision-host container running"
    url: "https://uis.sovereignsky.no"
  - text: "Local Kubernetes cluster running (Rancher Desktop)"
    url: "https://www.rancher.com/products/rancher-desktop"
