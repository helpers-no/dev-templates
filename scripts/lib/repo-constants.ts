/**
 * repo-constants.ts — Central source of repo identity for URL-building
 * helpers (currently: the Files dropdown GitHub links).
 *
 * If the repo ever moves or the default branch is renamed, change these
 * in one place and regenerate the registry.
 */

export const REPO_BASE_URL = 'https://github.com/helpers-no/dev-templates';
export const REPO_BRANCH = 'main';
